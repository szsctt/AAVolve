name: snakemake

on:
  push:
    branches:
      - '*'
    tags:
      - 'v*'
  pull_request:
    branches:
      - 'main'

jobs:
  snakemake:
    runs-on: ubuntu-latest
    steps:
      -
        name: Checkout
        uses: actions/checkout@v4
      -
        name: Install apptainer
        uses: eWaterCycle/setup-apptainer@v2
        with:
          apptainer-version: 1.3.6
      - 
        name: Create micromamba env
        uses: mamba-org/setup-micromamba@v1
        with:
          environment-file: deps/snakemake/snakemake.yml
      - 
        name: Run snakemake
        run: snakemake --cores 1 --config samples=tests/data/config/test_samples.csv --use-apptainer
        shell: micromamba-shell {0}
      - 
        name: Run snakemake with 'quickstart' command
        run: snakemake --cores 1 --use-apptainer --config read_file=tests/data/tests/data/reads/np-cc-aav2.fastq parent_file=tests/data/references/wtAAV2.fa seq_tech=np-cc sample_name=aav2-np-cc
        shell: micromamba-shell {0}

